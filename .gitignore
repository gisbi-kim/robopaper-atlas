# Large intermediate file (108 MB, over GitHub's 100 MB limit)
# — can be regenerated by running `python step2_openalex.py` (fast if checkpoint exists)
all_enriched.json

# CSV is redundant with xlsx (97 MB, same content). Kept out of repo.
icra_iros_ral_tro_rss_all.csv

# Logs
*.log

# Intermediate helper JSONs (generated on the fly)
_top100_list.json
_top100_by_year.json

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Claude Code local state
.claude/

# Personal reference notes (not for publication)
0. 기존 웹 요청 (참고용).md
